---
title: Midnight Import Demo
summary: A local markdown import sample for testing the filesystem preview and image handling path.
tags: [linux, containers, markdown]
slug: midnight-import-demo
theme_variant: midnight
hero_image: images/terminal-rig.svg
---

# Midnight Import Demo

This post exists to prove the filesystem importer path.

It should import as a normal article, keep the `midnight` theme, and rewrite relative assets to the local mounted content path.

![Terminal rig](images/terminal-rig.svg)

You can trigger the preview from `/admin`, filter on `linux`, and then import just this file if needed.
